import {useDispatch} from "react-redux";

const SectionListItem = ({todo}) => {
    const dispatch = useDispatch();
    return(
        <li className="list-group-item pt-3">

        </li>
    )
}

export default SectionListItem;